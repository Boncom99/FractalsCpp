#include <iostream>
#include "zoom.h"
using namespace std;

Zoom::Zoom(long double x, long double y, double scale, double percentage) : x(x), y(y), scale(scale), percentage(percentage)
{
}
Zoom::~Zoom()
{
}
